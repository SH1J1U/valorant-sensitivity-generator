# 0911 多游戏灵敏度生成器 · v0.3 使用说明

在 v0.2 的基础上，重点完善了动画和手感反馈。游戏换算逻辑没变，支持的游戏也没变。

## 本次更新：动画与手感反馈

### 打中目标终于有反馈了

以前准星碰到目标，画面什么都不发生——目标直接消失，下一个直接出现。v0.3 每次命中会立刻给你三样东西：

- 命中点向外扩散的红圈
- 一圈碎片粒子飞散
- 上方飘出 `+1 · 92ms`，告诉你这次用了多少毫秒

这是 v0.3 最值得看的一处，直接决定甩枪时的节奏感。

### 目标出现有过程了

目标不再是"啪"地闪出来，而是从小放大弹入，同时外圈扩散一圈浅色环。动态追踪的目标会留下渐隐拖尾，方便你判断它在往哪边走。

### 准星会跟着你的手走

- 移动越快，准星顺着移动方向轻微拉长，停下来就收回
- 命中瞬间准星放大一下再弹回

### 倒计时和剩余时间

- 开始前的倒计时数字每秒弹一下，下面多了一条进度条，让你知道还有多久正式开测
- 最后 5 秒，剩余时间会变红并持续脉冲，提醒你收尾

### 结果页不再"瞬间出现"

点"生成我的专属灵敏度"之后：

- 结果区域整体展开
- 大号灵敏度数字从 0 滚到你真实的值，eDPI、cm/360°、三档参考值同步滚动
- 可信度条重新从 0 跑到真实的百分比
- 换算表 6 行依次滑入，你选的主游戏那一行会高亮闪一下

### 其它细节

首屏和各个卡片进入时依次上浮；鼠标垫校准的数值会滚动到位；测试项完成的勾会弹出来；顶部步骤条会跟着真实进度走（个人习惯 → 扫幅校准 → 内嵌测试 → 多游戏数值）。

## 如果觉得晃眼 / 卡顿

v0.3 会自动尊重系统的"减弱动态效果"开关。打开后，上面这些动画会全部关掉或压到极短，包括瞄准测试里的特效。

- Windows：设置 → 辅助功能 → 视觉效果 → 关闭"动画效果"
- macOS：系统设置 → 辅助功能 → 显示 → 打开"减少动态效果"

## 直接打开

用 Edge / Chrome 打开：

`content/valorant-sensitivity-generator/index_v0.3.html`

成绩仍在本地计算，不上传。

## 支持游戏

- 无畏契约 VALORANT
- 三角洲行动 Delta Force
- CS2
- 守望先锋 Overwatch 2
- Apex 英雄
- 穿越火线 CF

所有游戏按同一物理手感换算，基准是 cm/360°，也就是鼠标转一整圈要移动多少厘米。

## 使用步骤

1. 在"主游戏"里选你最常玩或最想先校准的游戏。
2. 填鼠标 DPI、鼠标垫可用宽度、发力方式、握持方式、常见失误等个人习惯。
3. 先做"鼠标垫扫幅校准"，再完成悬停甩枪、微调控制、动态追踪 3 项核心测试。
4. 可选做"左右换向"。
5. 点"生成我的专属灵敏度"。

生成后：

- 主游戏推荐值会大号显示，可直接复制。
- "同手感换算表"会同时给出其他 5 个游戏的灵敏度、DPI、eDPI 和 cm/360°。
- 每个游戏后面都有单独的"复制"按钮。

## 灵敏度怎么算

生成器先根据你的习惯和测试表现算出目标 cm/360°，再按各游戏自己的灵敏度系数换算成游戏内数值。算法只会修正个人基准的 ±12%，避免单次测试状态导致极端值。

例如同一个手感：

- 无畏契约：约 0.350
- CS2 / 三角洲行动 / Apex / CF：约 1.113
- 守望先锋：约 3.71

具体数值会随你的 DPI、测试结果和鼠标垫空间变化。

## 应用说明

每个游戏的设置路径已经在结果页的"如何填入游戏"里按对应游戏列出。

保持 DPI 不变，把对应游戏的灵敏度复制进去。拿到推荐值后，先在训练场或练枪图适应 2–3 天，再按 0.01 左右微调。

v0.3 没有改动本地存储的键名，你在 v0.2 里填过的参数会自动带过来。

## 文件位置

在线直接用：

https://sh1j1u.github.io/valorant-sensitivity-generator/

仓库内文件：

- 生成器主体：`index_v0.3.html`
- 线上入口：`index.html`（自动跳转到 v0.3）
- 上一版（无动效，保留可直接对比）：`index_v0.2.html`
- 开源许可与署名：`LICENSE`、`NOTICE`
